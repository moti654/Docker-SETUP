services:
  mytorch:
    build:
      context: ./dockerfile
      dockerfile: Dockerfile
      shm_size: 32G
    image: mytorch:v1.0-12.4.1-devel-ubuntu22.04
    volumes:
      - type: bind
        source: "./"
        target: "/my-repo"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    ports:
      - "127.0.0.1:6006:6006"
    tty: true
